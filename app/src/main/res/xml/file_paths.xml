<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- For cache directory -->
    <cache-path
        name="temp_images"
        path="." />

    <!-- For files directory -->
    <files-path
        name="chat_images"
        path="images/" />

    <!-- For external files directory -->
    <external-files-path
        name="external_images"
        path="." />

    <!-- For external cache directory -->
    <external-cache-path
        name="external_cache_images"
        path="." />


    <files-path name="images" path="." />
    <cache-path name="cache" path="." />
    <external-files-path name="external_files" path="." />

</paths>